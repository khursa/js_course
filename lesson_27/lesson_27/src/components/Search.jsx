import React, { Component } from 'react';

class Search extends Component {
  render() {
    return (
      <input 
        className="search"
        placeholder="Search"
        type="text"        
        />
    );
  }
}

export default Search;