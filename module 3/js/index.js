import * as UIController from "ui_controller.js";

UIController.prototype.run = function changeInput() {
    focus.addEventListener('keydown', function() {
        let pattern = /[A-Za-z]{2,}/;
      });
};